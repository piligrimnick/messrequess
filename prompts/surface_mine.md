This is your MR — get it to approved. Fix whatever is blocking that (open threads, CI, conflicts) and reply to each thread with a concrete plan.
[[if threads]]
Unresolved threads ({count}):
{threads}[[else]]
No unresolved threads — check what else is blocking approval (CI, conflicts, missing reviewers).
[[end]]